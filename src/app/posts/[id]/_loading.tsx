// Renaming this component only to "loading.tsx", without the "_" it is going to be called automatically when the page is waiting for something
export default function Loading() {
    return <h1>Carregando...</h1>;
}